meta {
  name: execute-sql
  type: http
  seq: 2
}

post {
  url: {{HOST}}/sql/execute-task
  body: json
  auth: none
}

body:json {
  {
    "subTaskId": 2,
    "query": "DELETE FROM users WHERE age < 48; SELECT * FROM users;"
  }
}
