meta {
  name: execute-sql
  type: http
  seq: 2
}

post {
  url: http://localhost:4000/api/sql/execute-sql
  body: json
  auth: none
}

body:json {
  {
    "idTask": 2,
    "query": "DELETE FROM users WHERE age < 48; SELECT * FROM users;"
  }
}
